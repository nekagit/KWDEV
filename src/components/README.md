# components

React components for the app: UI primitives (ui), atoms (buttons, inputs, badges, etc.), molecules (cards, tabs, forms, tables, Kanban), organisms (page-level content and layout), and shared (CommandPalette, TerminalSlot, Dialog). Composed by app pages and layout.
