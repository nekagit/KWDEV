# lib

Shared application logic: API and Tauri abstraction, project/ideas/tickets fetching, download and copy helpers, focus-filter and keyboard shortcuts, run-history and formatting utilities. Used by components, store, and API routes. Test files live in `__tests__/`.
