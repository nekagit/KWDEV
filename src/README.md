# src

Frontend and application source: Next.js App Router pages and API routes, React components (atoms, molecules, organisms, shared, ui), shared logic (lib), global state (store), types, context, and static data. The app runs in the browser (Next dev/server) or inside the Tauri desktop shell (static export).
