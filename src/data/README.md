# data

Static data and templates: keyboard shortcuts, UI theme templates, test templates, February repos overview, and seed content. Read by components and API; not the runtime data directory (that lives in `data/` at repo root).
