/** Integrations page: define and download a single .env for Stripe, Google, Meta, etc. */
import { IntegrationsPageContent } from "@/components/organisms/IntegrationsPageContent";

export default function IntegrationsPage() {
  return <IntegrationsPageContent />;
}
