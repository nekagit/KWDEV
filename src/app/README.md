# app

Next.js App Router: page routes (layout, dashboard, projects, ideas, prompts, configuration, etc.) and API routes under `api/`. Pages render components; API routes serve data and server-side actions (analyze, generate, CRUD).
